{
  "name": "Python App Template",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {
      "version": "latest"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-python.debugpy",
        "charliermarsh.ruff"
      ]
    },
    "codespaces": {
      "openFiles": [
        "README.md"
      ]
    }
  },
  "onCreateCommand": {
    "install-poetry": "pip install poetry",
    "config-poetry": "poetry config virtualenvs.in-project true"
  },
  "postCreateCommand": {
    "git-safe": "git config --global --add safe.directory ${containerWorkspaceFolder}",
    "install-deps": "poetry install",
    "setup-precommit": "poetry run pre-commit install",
    "ready": "echo '✨ Dev container setup complete!'"
  },
  "containerEnv": {
    "PYTHONPATH": "${containerWorkspaceFolder}/src",
    "PYTHONUNBUFFERED": "1"
  },
  "remoteEnv": {
    "PATH": "/home/vscode/.local/bin:${containerEnv:PATH}:${containerWorkspaceFolder}/.venv/bin"
  },
  "forwardPorts": [],
  "shutdownAction": "stopContainer",
  "remoteUser": "vscode"
}
